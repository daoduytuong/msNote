<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.note2" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Note2"
        android:usesCleartextTraffic="true" >

        <activity android:name=".ActivitySetting" />
        <activity android:name=".ActivityADD" />
        <activity android:name=".ActivityNoteDetail" />
        <activity android:name=".ActivityNote" />
        <activity android:name=".ActivityDangKy" />
        <activity android:name=".ActiLogin" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" >
        </activity>
    </application>

</manifest>